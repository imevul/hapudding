# Runs when a release is published (e.g. manually). Release Please–created releases
# upload assets from .github/workflows/release-please.yml because GITHUB_TOKEN does not
# trigger workflows on `release: published` for events it caused.
name: Release assets

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  assets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.release.tag_name }}

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Build binaries and source archive
        env:
          TAG: ${{ github.event.release.tag_name }}
        run: |
          set -euo pipefail
          VERSION="${TAG#v}"
          mkdir -p dist
          for pair in linux/amd64 linux/arm64; do
            GOOS="${pair%%/*}"
            GOARCH="${pair##*/}"
            out="dist/hapudding-${VERSION}-linux-${GOARCH}"
            GOOS=$GOOS GOARCH=$GOARCH go build -trimpath -ldflags "-s -w -X main.version=${VERSION}" -o "$out" ./cmd/hapudding
          done
          git archive --format=zip --prefix="hapudding-${VERSION}/" -o "dist/hapudding-${VERSION}-source.zip" "${TAG}"
          (cd dist && sha256sum ./* > SHA256SUMS)

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          cd dist
          gh release upload "${{ github.event.release.tag_name }}" \
            hapudding-*-linux-* \
            hapudding-*-source.zip \
            SHA256SUMS \
            --clobber
